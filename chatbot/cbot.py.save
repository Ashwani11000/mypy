#for creating a chatbot
from chatterbot import ChatBot
#for creating a trainer
from chatterbot.trainers import ChatterBotCorpusTrainer

#creating a chatbot : All work that needs to be done by chatbot is done using this
cbot = ChatBot("mybot")

#creating a trainer which will train the chatbot using files given
trainer = ChatterBotCorpusTrainer(cbot)

#training the chat bot with english corpus
trainer.train("chatterbot.corpus.english")


def resonding(message):
    #Response generated by chat bot for the message passed
    chatbot_Response=chatbot.get_response(message)
    print(chatbot_Response)

user_inp=input("User: ")
chatterbot.corpus.english"while(user_inp !="exit"):
